// This file previously contained SVG layout utilities (geographicLayout,
// schematicLayout, latLngToSvg) used by the old metro-map.tsx component.
// The map is now unified in real-map.tsx (Leaflet) which handles all layout
// via Leaflet's native projection. This file can be safely removed in the
// future if no other code imports from it.
