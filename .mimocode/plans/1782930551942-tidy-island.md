# Separate line selector from branch selector on Stations tab

## Change
`components/stations-tab.tsx` — add labels above each chip group:
1. Add label "Line" / "خط" above the line filter chips (line 77)
2. Add label "Direction" / "مسیر" above the branch selector chips (line 96)

Both labels use existing `text-xs font-semibold uppercase tracking-wide text-muted-foreground` style (already used elsewhere in the app).

## Files Modified
- `components/stations-tab.tsx` — add two labels
