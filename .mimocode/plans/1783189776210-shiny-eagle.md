# Plan: UI/UX Fixes

## Issues to Fix

### 1. Fast trains on timetable (L5 only)
**File**: `components/station-timesheet.tsx`
- Already has Zap icon for express in the time chips - this works
- Also mark in station-detail.tsx DepartureRow (already has ⚡ badge)

### 2. Suggest fast train option
**File**: `components/route-panel.tsx` (SegmentCard)
- When showing next train callout, if there's a local train coming soon but a fast train arriving later that would get you to your destination faster, show a suggestion like:
  - "سریع: ۸:۳۵ — ۵ دقیقه زودتر" (Express: 8:35 — 5 min faster)
- Compare: local arrival time at destination vs fast train arrival time at destination
- Only for Line 5 (only line with express trains)

### 3. Space missing in Persian route text
**File**: `components/route-panel.tsx` line 310
- Current: `{isFa ? "در" : "in"} {persianDigits(next.minutesUntil, lang)} {isFa ? "دقیقه" : "min"}`
- The space between direction name and "در" might be missing in RTL rendering
- Fix: ensure proper spacing with explicit `\u00A0` or template literal spacing

### 4. Shorten map popup departure list
**File**: `components/station-detail.tsx`
- Currently shows 6 departures per line → reduce to 3
- Change `getStationDepartures(station.id, getCurrentDayType(), 6)` → 3

### 5. Remove buttons from map popup card
**File**: `components/station-detail.tsx`
- Remove the Timetable and Set Origin buttons from the map popup
- Keep just: station name, lines, interchange badge, amenities, and 3 departures

### 6. Clear popup when switching tabs
**File**: `app/page.tsx`
- The `selectedId` state persists across tabs
- When switching away from map tab, clear `selectedId`
- Add to the tab change handlers: `if (tab !== "map") setSelectedId(null)`

### 7. "Get to starting point" button more prominent
**File**: `app/page.tsx` (RouteView)
- Current: just a link with MapPin icon
- Make it a full-width primary button with bigger text

### 8. Warning dark mode colors
**File**: `components/route-panel.tsx`
- Current: `dark:bg-amber-950` and `dark:bg-red-950`
- Use CSS variable-based approach or softer colors:
  - Red warning: `dark:bg-red-950/80 dark:border-red-800/30`
  - Amber warning: `dark:bg-amber-950/80 dark:border-amber-800/30`
  - Use `dark:bg-muted` + colored left border for theme-friendly look

## Files to modify
- `components/station-detail.tsx` — shorten departures, remove buttons
- `components/route-panel.tsx` — fast train suggestion, space fix, dark mode warnings
- `app/page.tsx` — clear selectedId on tab change, prominent origin button

## Verification
1. Map popup shows only 3 departures, no buttons
2. Switching from map to route tab hides the popup
3. Fast train suggestion appears on Line 5 routes
4. Persian text has proper spacing
5. Dark mode warnings look theme-friendly
