# Fix: Show total estimated time (including waits)

## Problem
Stat card shows `travelTimeOnly` (e.g., 42 min) instead of `estimatedSeconds` (e.g., 254 min).
The code already calculates `totalMinutes` correctly using the user's formula: w1 + x1 + 4 + w2 + x2.

## Fix
Change stat card from `travelMins` to `mins` (total time including all waits and transfers).

**File**: `components/route-panel.tsx` — change `travelMins` to `mins` in the time stat card.
