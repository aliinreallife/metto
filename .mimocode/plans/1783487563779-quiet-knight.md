# Add MCP Resources + Prompts

## Resources to add
1. `metro://stations` — full station list as JSON
2. `metro://lines` — line info with colors and station counts
3. `metro://station/{id}` — individual station details (template resource)

## Prompts to add
1. `plan-route` — "Plan a route from {origin} to {destination}"
2. `station-info` — "Tell me about {station}"
3. `find-nearest` — "Find metro near {location}"

## File to modify
- `app/api/mcp/route.ts` — add resources and prompts after tools
- `mcp-server.ts` — same changes for local stdio server

## Verification
1. Build passes
2. Smithery discovers 4 tools + 3 resources + 3 prompts
