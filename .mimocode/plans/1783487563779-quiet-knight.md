# Keep origin/dest across all tabs

## Change
In `app/nav.tsx`, make ALL nav links include `?from=X&to=Y` when route is set (not just the map link).

## File
- `app/nav.tsx` — remove the `dynamic` flag from NAV_ITEMS, apply `getMapHref()` logic to all items

## Verification
1. Build passes
2. Set route on `/` → click Stations tab → URL shows `/stations?from=X&to=Y`
3. Click Nearby tab → URL shows `/nearby?from=X&to=Y`
4. Click Map tab → URL shows `/map?from=X&to=Y`
5. Click Route tab → route is still there
